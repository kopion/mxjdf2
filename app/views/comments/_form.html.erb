<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <% :commenter %>
  </p>
  <p>
    <% f.label :body %><br />
    <%= f.text_area :body, rows: 5  %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>